# Plan 22-01: Fuzzy Match Utility

**Phase:** 22 — Screenshot Pipeline
**Type:** feature
**Complexity:** low
**Depends on:** none

## Goal

A standalone `nameSimilarity()` function and `findBestPlayerMatch()` helper exist in `src/lib/fuzzy-match.ts`, fully typed, with no external dependencies.

## Success Criteria

- [ ] `src/lib/fuzzy-match.ts` exists and exports `nameSimilarity` and `findBestPlayerMatch`
- [ ] `nameSimilarity('Josh Allen', 'josh allen')` returns `1.0` (exact match after normalize)
- [ ] `nameSimilarity('Josh Allen', 'Josh')` returns `0.85` (substring include)
- [ ] `nameSimilarity('Josh Allen', 'Jordan Bell')` returns a value between `0` and `1` computed from character set overlap
- [ ] `findBestPlayerMatch('Ja\'Marr Chase', players)` returns the Player with the highest similarity score above the 0.4 threshold, or `null` if none qualify
- [ ] TypeScript compiles with no new errors (`npx tsc --noEmit` from `apps/desktop`)

## Tasks

### Task 1: Create `src/lib/fuzzy-match.ts`

**File:** `apps/desktop/src/lib/fuzzy-match.ts` (new file)
**Action:** create

Create the file with the following exact implementation. No external npm packages — this is entirely hand-rolled.

```typescript
import type { Player } from '@dynasty-os/core-types';

/**
 * Normalizes a name string for comparison:
 *  - lowercases
 *  - strips punctuation (apostrophes, hyphens, periods)
 *  - collapses multiple spaces to one
 *  - trims
 */
function normalizeName(name: string): string {
  return name
    .toLowerCase()
    .replace(/[''.\-]/g, '')
    .replace(/\s+/g, ' ')
    .trim();
}

/**
 * Returns a similarity score in [0, 1] between two player name strings.
 *
 * Scoring tiers (evaluated in order — first match wins):
 *   1.0  — normalized strings are identical (exact match)
 *   0.85 — one normalized string is a substring of the other
 *   n    — character set overlap: |intersection| / max(|setA|, |setB|)
 *
 * Character set is the unique set of characters after normalization
 * (spaces included so "jo" and "joe" differ slightly).
 */
export function nameSimilarity(a: string, b: string): number {
  const na = normalizeName(a);
  const nb = normalizeName(b);

  if (na === nb) return 1.0;
  if (na.includes(nb) || nb.includes(na)) return 0.85;

  const setA = new Set(na.split(''));
  const setB = new Set(nb.split(''));
  let intersection = 0;
  for (const ch of setA) {
    if (setB.has(ch)) intersection++;
  }
  const maxSize = Math.max(setA.size, setB.size);
  return maxSize === 0 ? 0 : intersection / maxSize;
}

/**
 * Finds the best-matching Player from an array given a raw name string
 * (e.g., from a parsed screenshot).
 *
 * Compares `candidateName` against `${player.firstName} ${player.lastName}`
 * for every player. Returns the player with the highest similarity score,
 * provided that score is >= 0.4. Returns null if no player meets the threshold.
 */
export function findBestPlayerMatch(
  candidateName: string,
  players: Player[]
): { player: Player; score: number } | null {
  if (!candidateName.trim() || players.length === 0) return null;

  let best: { player: Player; score: number } | null = null;

  for (const player of players) {
    const fullName = `${player.firstName} ${player.lastName}`;
    const score = nameSimilarity(candidateName, fullName);
    if (score >= 0.4 && (!best || score > best.score)) {
      best = { player, score };
    }
  }

  return best;
}
```

## Verification

- [ ] `npx tsc --noEmit` (run from `apps/desktop`) exits with no errors
- [ ] Manually confirm exports exist: `grep -n "export function" apps/desktop/src/lib/fuzzy-match.ts` returns both `nameSimilarity` and `findBestPlayerMatch`
